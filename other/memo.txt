如何在 `data-manager.js` 中優化搜尋邏輯，讓它支援多關鍵字搜尋（例如「Waboku 2021」）？
燈箱大圖寬高
如何為 UIManager 加入一個全屏加載動畫（Loading Spinner）的顯示與隱藏功能？
如果我想在不同的分頁（Gallery vs Modal）中使用不同的 Toast 顯示位置，該如何調整 UIManager？


1. index中存在一些獨立的函數，可以整合到js文件中嗎？
2. 還有photoswipe可以改成esm引用方式
import PhotoSwipeLightbox from 'photoswipe/dist/photoswipe-lightbox.esm.js';
const lightbox = new PhotoSwipeLightbox({
  gallery: '#my-gallery',
  children: 'a',
  pswpModule: () => import('photoswipe/dist/photoswipe.esm.js')
});
lightbox.init();
還有PhotoSwipeDynamicCaption也改成esm引用方式，如何整合
import PhotoSwipeDynamicCaption from 'https://unpkg.com/photoswipe-dynamic-caption-plugin/photoswipe-dynamic-caption-plugin.esm.js';
3. 另外masonry和imagesloaded我看官網沒查到有ESM的版本，也可以改嗎？

你知道https://www.neobrutalism.dev/這個組件庫嗎，如果我需要從bootstrap遷移到neobrutalism需要做哪些準備，先不動代碼，想了解怎麼實現

但是neobrutalism跟bootstrap不一樣，我看官網說是基於shadcn這個組件庫，這個組件庫不是單純引用CSS和JS，需要使用NPM，這樣該如何整合

建議： 安裝 @tanstack/react-query。它可以自動處理 Loading 狀態、錯誤重試、以及分頁數據的快取。

更好的方式： 結合 虛擬滾動 (Virtual List)。只渲染畫面上看得到的 Card。這對於畫廊類應用是頂級的優化。

如果你的圖片高度不一（真正的瀑布流）： 推薦使用 react-masonry-css。它基於 CSS 欄位（Columns）實現，比傳統的 Masonry.js 快得多且更符合 React 宣告式寫法。